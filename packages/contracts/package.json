{
  "name": "@fhevm-sdk/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for FHEVM SDK examples",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "keywords": ["fhevm", "smart-contracts", "solidity", "fhe"],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.16",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "fhevm": "^0.5.0",
    "fhevm-core-contracts": "^0.5.0"
  }
}
