{
  "name": "fhevm-weather-aggregator-sdk",
  "version": "1.0.0",
  "description": "Universal FHEVM SDK with Weather Aggregator example",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build:sdk && npm run build:contracts",
    "build:sdk": "cd packages/fhevm-sdk && npm run build",
    "build:contracts": "cd packages/contracts && npx hardhat compile",
    "deploy:contracts": "cd packages/contracts && npx hardhat run scripts/deploy.js --network sepolia",
    "dev:example": "cd examples/weather-aggregator && npx http-server . -c-1 --cors",
    "install:all": "npm install && npm run install:packages",
    "install:packages": "cd packages/fhevm-sdk && npm install && cd ../contracts && npm install",
    "test": "cd packages/contracts && npx hardhat test"
  },
  "keywords": [
    "fhevm",
    "fhe",
    "sdk",
    "weather",
    "privacy",
    "zama"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
